<template>
  <qas-single-view class="container spaced" entity="<%= props.name %>">
    <template v-slot:header="{ result }">
      <qas-page-header :breadcrumbs="breadcrumbs" title="__MUDAR__NOME__">
        <qas-btn hide-mobile-label icon="o_create" label="Editar __MUDAR__NOME__" :to="{ name: '<%= props.editName %>', params: { id: result.uuid } }" />
      </qas-page-header>
    </template>
    <template v-slot="{ fields, result }">
      <qas-box>
        <qas-grid-generator :fields="fields" hide-empty-result :result="result" />
      </qas-box>
    </template>
  </qas-single-view>
</template>

<script>
export default {
  computed: {
    breadcrumbs () {
      return [
        { label: 'Lista de __MUDAR__NOME__', routeName: '<%= props.listName %>' },
        { label: '__MUDAR__NOME__' }
      ]
    }
  }
}
</script>

<style lang="scss">
.page-<%= props.name %>-single {}
</style>
