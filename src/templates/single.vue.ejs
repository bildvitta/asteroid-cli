<template>
  <qas-single-view
    v-model:fields="fields"
    v-model:result="result"
    entity="<%= props.name %>"
  >
    <template #header>
      <!-- TODO: Validar botão de deletar onde irá ficar -->
      <qas-page-header
        :breadcrumbs="breadcrumbs"
        title="__MUDAR__NOME__"
      >
        <qas-btn
          hide-mobile-label
          icon="o_create"
          label="Editar __MUDAR__NOME__"
          padding="md"
          :to="{ name: '<%= props.editName %>', params: { id: result.uuid } }"
        />
      </qas-page-header>
    </template>
    <template #default>
      <qas-box>
        <qas-grid-generator
          :fields="fields"
          hide-empty-result
          :result="result"
        />
      </qas-box>
    </template>
  </qas-single-view>
</template>

<script>
export default {
  name: '<%= props.singleName %>',

  data () {
    return {
      fields: {},
      result: {}
    }
  },

  computed: {
    breadcrumbs () {
      return [
        { label: 'Lista de __MUDAR__NOME__', routeName: '<%= props.listName %>' },
        { label: '__MUDAR__NOME__' }
      ]
    }
  }
}
</script>
